language: ruby
rvm:
 - 2.2.7
# safelist
branches:
  only:
    - master
services:
  - postgresql
before_script:
  - psql -c "CREATE USER sesame WITH PASSWORD 'bar';" -U postgres
  - psql -c "ALTER ROLE sesame WITH SUPERUSER;" 
  - psql -c 'create database show_caster_test;' -U sesame
