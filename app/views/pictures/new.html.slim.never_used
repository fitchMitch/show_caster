.container
  .page-header
    = link_to pictures_path, :class => 'btn btn-default' do
      span.glyphicon.glyphicon-list-alt
        = " #{t("back")}"
    h2= t("pictures.new")

  = render 'form', {imageable: @imageable, picture: Picture.new}
