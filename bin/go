#!/usr/bin/env ruby

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

puts "\n== starting docker up with byebug facilities =="
system! "sudo docker-compose up -d"
system! "sudo docker attach show_caster_app_1"