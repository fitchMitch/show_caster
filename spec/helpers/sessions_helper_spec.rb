require 'rails_helper'

RSpec.describe SessionsHelper, type: :helper do
  # include Sessions::LoginHelper
  # describe "#logout" do
  #   let!(:admin) { create(:user, :registered)}
  #   before do
  #     log_in(admin)
  #     helper.logout
  #   end
  #
  #   it 'should set the session to nil' do
  #     expect(session[admin.id]). to be nil
  #     expect(assign(:current_user)).to eq nil
  #     expect(response).to redirect_to root_url
  #   end
  # end
end
